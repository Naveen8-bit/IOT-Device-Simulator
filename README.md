# IoT Device Simulator

This project is a simple simulation of a real-world IoT device built using Python.

The system continuously generates temperature data, handles possible sensor failures, and sends the data to an MQTT broker. The project is designed with a modular structure, making it easy to extend and deploy on platforms like Raspberry Pi.

It also includes proper logging, configuration management using YAML, and error handling to make the system more realistic.
